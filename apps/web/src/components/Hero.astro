---
---

<section class="hero section">
  <div class="container hero-inner">
    <div class="hero-badge">Open Source AI Infrastructure</div>
    <h1 class="hero-title">
      The AI Runtime<br />for <span class="gradient-text">Every Browser</span>
    </h1>
    <p class="hero-subtitle">
      One API. Local WebGPU when possible. Cloud when needed.<br />
      Your code doesn't change.
    </p>
    <div class="hero-actions">
      <a href="/docs/getting-started/quick-start" class="btn btn-primary">Get Started</a>
      <a href="/playground" class="btn btn-secondary">Live Playground</a>
    </div>
    <div class="hero-visual">
      <div class="flow-line"></div>
      <div class="flow-labels">
        <span class="flow-label flow-local">WebGPU Local</span>
        <span class="flow-label flow-sdk">SDK</span>
        <span class="flow-label flow-cloud">Cloud API</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: 10rem;
    padding-bottom: 6rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 800px;
    height: 600px;
    background: radial-gradient(
      ellipse at center,
      rgba(124, 58, 237, 0.12) 0%,
      rgba(6, 182, 212, 0.06) 40%,
      transparent 70%
    );
    pointer-events: none;
  }

  .hero-inner {
    position: relative;
  }

  .hero-badge {
    display: inline-block;
    padding: 0.375rem 1rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    font-size: 0.8125rem;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
    letter-spacing: 0.02em;
  }

  .hero-title {
    font-size: 4rem;
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin-bottom: 2.5rem;
    max-width: 560px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 5rem;
  }

  .hero-visual {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .flow-line {
    width: 400px;
    height: 2px;
    background: var(--gradient-hero);
    border-radius: 1px;
    position: relative;
    opacity: 0.6;
  }

  .flow-line::before {
    content: '';
    position: absolute;
    top: -4px;
    left: -4px;
    right: -4px;
    bottom: -4px;
    background: var(--gradient-hero);
    filter: blur(8px);
    opacity: 0.4;
    border-radius: 4px;
  }

  .flow-labels {
    display: flex;
    justify-content: space-between;
    width: 400px;
  }

  .flow-label {
    font-size: 0.75rem;
    font-family: var(--font-mono);
    padding: 0.25rem 0.625rem;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
    background: var(--color-bg-elevated);
  }

  .flow-local { color: var(--color-accent); }
  .flow-sdk { color: var(--color-text); }
  .flow-cloud { color: var(--color-cyan); }

  @media (max-width: 768px) {
    .hero {
      padding-top: 7rem;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.0625rem;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }

    .flow-line,
    .flow-labels {
      width: 280px;
    }
  }
</style>
