---
const problems = [
  {
    icon: '&#9881;',
    title: 'Hardware Fragmentation',
    description:
      'An 8GB VRAM gaming laptop runs 8B models. A 2GB phone runs nothing. Developers are forced to write different logic for every device class.',
    color: 'var(--color-accent)',
  },
  {
    icon: '&#128230;',
    title: 'Model Distribution',
    description:
      '2-4GB model files for end users? HTTP cache gets cleared anytime. CDN bandwidth costs add up. There is no standard for client-side model delivery.',
    color: 'var(--color-cyan)',
  },
  {
    icon: '&#128268;',
    title: 'Fragmented DX',
    description:
      'Local inference uses WebGPU APIs. Cloud uses OpenAI SDK. Two interfaces, two error handling patterns, two type systems. Twice the complexity.',
    color: 'var(--color-amber)',
  },
];
---

<section class="section problems">
  <div class="container">
    <p class="section-label">The Problem</p>
    <h2 class="section-title">Browser AI is stuck in the tooling gap</h2>
    <div class="problems-grid">
      {problems.map((p) => (
        <div class="card problem-card">
          <div class="problem-icon" style={`color: ${p.color}`} set:html={p.icon} />
          <h3 class="problem-title">{p.title}</h3>
          <p class="problem-desc">{p.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .problems {
    border-top: 1px solid var(--color-border-subtle);
  }

  .section-label {
    font-size: 0.8125rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-accent);
    margin-bottom: 0.75rem;
  }

  .problems-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .problem-card {
    background: var(--gradient-card);
    border: 1px solid var(--color-border);
  }

  .problem-icon {
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }

  .problem-title {
    font-size: 1.0625rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .problem-desc {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .problems-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
